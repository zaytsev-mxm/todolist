services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      PGPORT: ${PORT_DB}
    ports:
      - "${PORT_DB}:${PORT_DB}" # "host:container"
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - app-network

  app:
    # keep either image: ... or your local build: block
    # image: zaytsevmxm/todolist-backend:${DOCKER_IMAGE_VERSION}
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - db
    ports:
      - "${PORT_APP}:${PORT_APP}" # "host:container"
    volumes:
      - .:/app
    networks:
      - app-network
    command: "sh ./tools/dev.sh"

volumes:
  db-data:

networks:
  app-network: